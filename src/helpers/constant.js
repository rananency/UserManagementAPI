const errorMessages={
    INVALId_USER_ID:'Invalid user ID',
    EMAIL_EXIST: 'Email already exist',
    USER_NOT_FOUND: "User not found: {userId}",
    OPERATION_FAILED:"Error while {method} user:"
}

const messages = {
    OPERATION_SUCCESS:'User details {method} successfully'
}

module.exports = {
    errorMessages,
    messages
}